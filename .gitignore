# OS
.DS_Store
Thumbs.db

# Secrets — never commit
.env
.env.*
*.pem
*.key
credentials.json

# Editor / local tooling
*.swp
*~

# Node / Next (root viewer entry)
node_modules/
.next/
out/
next-env.d.ts
.vercel/

# Generated asset link/copy under viewer public
viewer/public/assets
.vercel
.env*

# Reference media — source video stays local (too large)
research/**/*.mp4
research/**/*.m4a
research/**/*.wav

# Inbox voice recordings — keep local; direction notes in feedback/ are tracked
feedback/inbox/**/*.m4a
feedback/inbox/transcript/

# Mind-meld runtime (locks / hook logs)
mind/.sync.log
mind/.sync.lock
mind/.append.lock
mind/.sync.stamp
